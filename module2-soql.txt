SOQL---


1. done
Write SOQL Select all student Name,Dob,Sex where LastName starts with “S”
SELECT Name,LastName__c, DOB__c, Sex__c From Student__c where LastName__c like 'S%'

2. done
Select All students where ClassName starts with “X”
SELECT class__r.Name, Name,LastName__c, DOB__c, Sex__c FROM Student__c where class__r.Name like 'X%'

3. done
Select all Student and ClassName
SELECT class__r.Name, Name,LastName__c, DOB__c, Sex__c FROM Student__c 

4. done
Select all Classes with students in every class as subquery.
SELECT name , (select firstname__c from Students__r)  FROM class__C

5. done
Select all students where class teacher name starts with “S”
select name,class__r.ClassTeacher__r.name from student__c where class__r.ClassTeacher__r.name like 'S%'


6. done
All students where DOB > Year 2000
SELECT firstname__c,LastName__c, CALENDAR_YEAR(dob__c) FROM Student__c group by  firstname__c,LastName__c,CALENDAR_YEAR(dob__c)
having CALENDAR_YEAR(dob__c) > 2000

7.
All Leads Where ModifiedDate in current month
select name,lastmodifieddate from lead  where lastmodifieddate  = THIS_MONTH

8. done *
All Classes with Students as subquery where student Name Starts with “S”
SELECT name , (select firstname__c from Students__r where firstname__c like 'S%')  FROM class__C 

9. done
All Teachers who have Hindi and English both subjects
select name,teacher__r.name,subject__C from teach__C where subject__c includes ('Hindi') and subject__c includes ('English') 

10.
All students who have hindi or maths
select id,name from student__C where class__C in (SELECt classes__c FROM teach__C where subject__c includes ('Hindi'))

11. done
Select no. of students per class
select class__r.name,count(id) from student__c group by class__r.name

12. done
Monthly Birthday Count of students with sorting
select calendar_month(dob__C),Count(id)  from student__c group by calendar_month(dob__C) order by calendar_month(dob__C) asc


SOSL:-
1. done
Find all persons with name as “Joe” i.e (Teachers or Students)
FIND {joe} IN Name Fields 
RETURNING Student__c(name),Contact(name)

2. done
Find all Male persons with name as “Joe” i.e (Teachers or Students)
FIND {joe} IN Name Fields 
RETURNING Student__c(name where sex__C = 'Male'),Contact(name)